[gd_resource type="ShaderMaterial" load_steps=2 format=3 uid="uid://dfv4hhqft6a3x"]

[sub_resource type="Shader" id="1"]
code = "shader_type canvas_item;

uniform float amount: hint_range(0.0, 5.0);

uniform sampler2D SCREEN_TEXTURE : hint_screen_texture, filter_linear_mipmap_anisotropic;

void fragment() {
	COLOR.rgb = mix(textureLod(SCREEN_TEXTURE, SCREEN_UV, amount).rgb, COLOR.rgb, COLOR.a);
	COLOR.a = 1.0;
}"

[resource]
shader = SubResource("1")
shader_parameter/amount = 3.5
